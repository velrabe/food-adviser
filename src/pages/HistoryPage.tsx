export function HistoryPage() {
  return (
    <div className="page">
      <h1>История генераций</h1>
      <p className="muted">
        Список записей из <code>generation_history</code> и действия restore / apply — этап 2–3.
      </p>
    </div>
  )
}
